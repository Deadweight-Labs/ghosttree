# Provenance

Adapted from the Deadweight Labs Clay design primitives at commit `a7b66e5`
(2026-08-23). Product-specific comments were normalized for this repository;
the physics values are unchanged. Ghosttree-specific design belongs in
`../theme.css` and layout rules in `../app.css`.
